@Library('jenkinsLibs') _
pipeline {
    agent any
    stages {
        stage('saludar'){
            steps{
                script{
                    mover.saludar()
                }
            }
        }
    }
}