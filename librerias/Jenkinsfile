@Library('jenkinsLibs') _
pipeline {
    agent any
    stages {
        stage('crear'){
            steps{
                script{
                    echo "Creando carpeta..."
                    mover.crearDir()
                }
            }
        }
    }
}