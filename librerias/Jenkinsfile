@Library('jenkinsLibs') _
def saludar = new Saludar()
pipeline {
    agent any
    stages {
        stage('saludar'){
            steps{
                script{
                    saludar.saludar()
                }
            }
        }
    }
}