@Library('jenkinsLibs') _
pipeline {
    agent any
    stages {
        stage('crear'){
            steps{
                script{
                    mover.crearDirectorio()
                }
            }
        }
    }
}