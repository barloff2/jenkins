@Library('jenkinsLibs') _
pipeline {
    agent any
    stages {
        stage('example'){
            steps{
                script{
                    saludar()
                }
            }
        }
    }
}